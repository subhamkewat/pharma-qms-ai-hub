# Database configuration
# PostgreSQL: postgresql://user:password@localhost:5432/pharma_complaints
# SQLite fallback: sqlite:///./complaints.db
DATABASE_URL=sqlite:///./complaints.db

# Groq API Key for Gemma2-9B-IT (Required for live AI, falls back to simulated responses if empty)
GROQ_API_KEY=your_groq_api_key_here
PORT=8000
HOST=127.0.0.1
